# AI-REDGIO5.0-E2C-OS-Smart-Data-Enabler

This guide explains how to deploy the application using Docker Compose. The application is packaged as a prebuilt Docker image, and all necessary configurations are provided in the .env file.

Prerequisites
Before you begin, ensure the following are installed on your system:

1. Docker:

Install Docker by following the official guide: Install Docker.

2.Docker Compose:

Install Docker Compose by following the official guide: Install Docker Compose.
Firewall Configuration:

